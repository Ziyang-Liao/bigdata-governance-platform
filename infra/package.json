{
  "name": "bigdata-governance-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "deploy": "cdk deploy --all --profile temp-account",
    "destroy": "cdk destroy --all --profile temp-account",
    "diff": "cdk diff --profile temp-account"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "typescript": "~5.6.0",
    "ts-node": "^10.9.2",
    "aws-cdk": "^2.170.0"
  }
}
